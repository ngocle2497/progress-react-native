const DEFAULT_RADIUS: number = 40;
const DEFAULT_STROKE_WIDTH: number = 4;
const DEFAULT_STROKE_COLOR: string = '#0057e7';
const DEFAULT_BG_STROKE_COLOR: string = 'transparent';
const DEFAULT_IS_RADIUS: boolean = false;
const DEFAULT_SHOW_TEXT: boolean = false;
const DEFAULT_TEXT_CONCAT: string = '';
export {
    DEFAULT_RADIUS,
    DEFAULT_STROKE_WIDTH,
    DEFAULT_STROKE_COLOR,
    DEFAULT_BG_STROKE_COLOR,
    DEFAULT_IS_RADIUS,
    DEFAULT_SHOW_TEXT,
    DEFAULT_TEXT_CONCAT
}